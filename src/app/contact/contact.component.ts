import { ChangeDetectionStrategy, Component, ElementRef, inject } from '@angular/core';
@Component({selector:'app-contact',standalone:true,templateUrl:'./contact.component.html',styleUrl:'./contact.component.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class ContactComponent{private el=inject(ElementRef<HTMLElement>);calendly='https://calendly.com/your-username/meeting';onSubmit(event:SubmitEvent){const form=event.target as HTMLFormElement;const controls=Array.from(form.querySelectorAll<HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement>('input, textarea, select'));for(const c of controls){if(typeof c.value==='string')c.value=c.value.trim();c.setCustomValidity('');if(c.required&&!c.value)c.setCustomValidity('Please fill this field.')}if(!form.checkValidity()){event.preventDefault();form.reportValidity();}}}
